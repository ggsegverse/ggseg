% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-brain-polygon.R
\name{annotate_brain_polygon}
\alias{annotate_brain_polygon}
\title{Add view labels to a polygon-path brain plot}
\usage{
annotate_brain_polygon(
  atlas,
  position = position_brain_polygon(),
  hemi = NULL,
  view = NULL,
  size = 3,
  colour = "grey30",
  family = "mono",
  nudge_y = 0,
  ...
)
}
\arguments{
\item{atlas}{A `ggseg_atlas` object carrying a `$data$polygons` slot.}

\item{position}{A [position_brain_polygon()] spec matching the one used
in [geom_brain_polygon()].}

\item{hemi}{Character vector of hemispheres to include. If `NULL`
(default), all hemispheres are included.}

\item{view}{Character vector of views to include. If `NULL` (default),
all views are included.}

\item{size}{Text size in mm (default `3`).}

\item{colour}{Text colour (default `"grey30"`).}

\item{family}{Font family (default `"mono"`).}

\item{nudge_y}{Additional vertical offset for labels (default `0`).}

\item{...}{Additional arguments passed to [ggplot2::annotate()].}
}
\value{
A ggplot2 annotation layer.
}
\description{
Annotates each brain view with a text label positioned above the view's
bounding box. For cortical atlases, labels show hemisphere and view
(e.g. "left lateral"). For subcortical and tract atlases, labels show
the view name directly.
}
\details{
Labels respect the layout applied by [position_brain_polygon()], so the
same `position` argument should be passed to both [geom_brain_polygon()]
and `annotate_brain_polygon()`.
}
\examples{
\dontrun{
library(ggplot2)
poly <- ggseg.formats::as_polygon_atlas(dk())
pos <- position_brain_polygon(hemi ~ view)
ggplot() +
  geom_brain_polygon(atlas = poly, position = pos, show.legend = FALSE) +
  annotate_brain_polygon(atlas = poly, position = pos)
}
}
