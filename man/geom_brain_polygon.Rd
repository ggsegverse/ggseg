% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-brain-polygon.R
\name{geom_brain_polygon}
\alias{geom_brain_polygon}
\title{Plot brain atlas regions from the polygon representation (sf-optional)}
\usage{
geom_brain_polygon(
  mapping = aes(),
  data = NULL,
  atlas,
  hemi = NULL,
  view = NULL,
  position = position_brain_polygon(),
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by `ggplot2::aes()`.}

\item{data}{A data.frame containing variables to map. If `NULL`, the
atlas is plotted without user data.}

\item{atlas}{A `ggseg_atlas` object with 2D geometry (sf or polygons).}

\item{hemi}{Character vector of hemispheres to include.}

\item{view}{Character vector of views to include.}

\item{position}{Position adjustment. Defaults to [position_brain_lite()],
which lays views out horizontally without sf. Pass `"identity"` to use
the polygons' raw coordinates.}

\item{show.legend}{Logical. Should this layer be included in legends?}

\item{inherit.aes}{Logical. If `FALSE`, overrides the default aesthetics.}

\item{...}{Additional arguments passed to `ggplot2::geom_polygon()`.}
}
\value{
A list of ggplot2 layer and coord objects.
}
\description{
Renders a `ggseg_atlas` via `ggplot2::geom_polygon` using the polygon
representation from `ggseg.formats::atlas_polygons()`. Works on any atlas;
a polygon-backed atlas (e.g. from `ggseg.formats::as_polygon_atlas()`)
renders without requiring the `sf` package or its GDAL/GEOS/PROJ system
libraries.
}
\details{
Hole geometry round-trips correctly via the `subgroup` aesthetic
(`grid::pathGrob` even-odd fill).
}
\examples{
\dontrun{
library(ggplot2)
poly <- ggseg.formats::as_polygon_atlas(dk())
ggplot() + geom_brain_polygon(atlas = poly)
}
}
